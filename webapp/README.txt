To run:

1. Have node, bower and grunt-cli installed.
2. In the root for webapp, run "npm install".
3. Then run "bower install".
4. "grunt serve" to serve the app locally on port 9000. Make sure to disable chrome web security to avoid cors issue.
5. "grunt build" to build from the code. The build is copied to /dist. 
